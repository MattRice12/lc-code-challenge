<%= form_for(comment) do |f| %>
  <div class="errors">
    <ul>
      <% comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>

  <div class="form-body">
    <%= f.text_area :text %>
    <%= f.submit "Post", class: "post-button" %>
  </div>
<% end %>
